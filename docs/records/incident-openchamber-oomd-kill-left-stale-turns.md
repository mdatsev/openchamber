# Incident: OpenChamber OOM kill left stale turns

- **Type:** record
- **Purpose:** Record why multiple OpenChamber sessions stopped progressing without showing their unexpected-interruption state.
- **When to read:** When sessions retain unfinished assistant turns or running tools after the managed OpenCode server restarts, especially following system memory pressure.

## Resolution (TL;DR)

On 2026-08-11, `systemd-oomd` killed the official `openchamber.service` cgroup after it reached 8.3 GB peak memory while several sessions and memory-heavy validation commands were active. Systemd restarted OpenChamber six seconds later, but the killed requests could not resume and six assistant turns remained unfinished in the shared OpenCode database. The same failure mode recurred for the separated `opencode.service` on 2026-08-18 during host-wide swap exhaustion. That recurrence exposed two remaining containment gaps: aggregate command enforcement ignores swap pressure and leaves populated command scopes from a killed OpenCode process running, while systemd ignores the user-owned OpenCode cgroup's `ManagedOOMPreference=avoid` when ranking swap victims. The UI's interruption classifier also returned early for reconciled explicit idle state; preserving authoritative snapshot active-set membership fixes that presentation gap. Affected sessions must still be resumed with a new prompt, preferably one at a time after inspecting partial work.

## Status

workaround

## Desired recovery topology

The user clarified that the goal is development continuity rather than multiple equivalent OpenChamber control planes: a custom development instance must be restartable or allowed to break while long-running OpenCode sessions continue, and the regular OpenChamber must be able to reconnect to and continue those same sessions. The selected first-stage design is one stable external OpenCode runtime and exactly one active OpenChamber surface, with an independent switch command replacing regular and custom surfaces without stopping OpenCode. OpenChamber-owned automation continuity and external plugin provisioning remain separate follow-up boundaries.

## Timeline + investigation

- At 2026-08-18 07:00:41 EEST, `systemd-oomd` killed seven processes in `opencode.service`. Oomd reported 14,893,346,816 of 16,532,684,800 bytes of host memory and 4,294,733,824 of 4,294,963,200 bytes of swap in use; the selected service was the largest swap consumer at 1.3 GB despite `ManagedOOMPreference=avoid`. Systemd reported a 7.1 GB memory peak and 1.1 GB swap peak, then restarted OpenCode at 07:00:47. Parent session `ses_fedf2f69cffeQaMb7K47J01GJq` had been waiting since 05:54:19 on child sessions `ses_fedb06f2affeehOw1Px2QTkyOq` and `ses_fedb06f18ffe5pmgNrT52AzChb`; the children persisted an empty post-compaction assistant turn and unfinished reasoning respectively within seven seconds of the kill. After restart, `/api/session/status` was empty while the parent still stored both task parts as `running`, confirming that none of those turns remained executable. The parent and children retained their prior messages and can be continued explicitly, preferably sequentially and with instructions to synthesize existing evidence before starting more memory-heavy research.
- The August 18 event was not a recurrence of the earlier multi-gigabyte command-output amplification. The three affected session aggregates contain about 90 MB of event data in total. Immediately before the kill, both research children were streaming model turns; OpenCode started automatic compaction for `ses_fedb06f2affeehOw1Px2QTkyOq` at 07:00:41.529, 140 ms before oomd selected the service. The compaction child had accumulated 405 messages, 2,683 parts containing 45.3 MB of current data, and a latest step context of 372,348 tokens. The journal does not isolate one allocation inside OpenCode, but the final pressure occurred in OpenCode's own model/context path rather than in a command process that a per-command cgroup could kill.
- Six orphaned TypeScript LSP command scopes started on August 12 under OpenCode PID 817179 were still populated after that process's August 13 OOM kill and the later OpenCode restarts. Five appeared immediately below `opencode.service` in oomd's August 18 swap ranking. At investigation time the six scopes retained about 1.1 GB resident memory, 1.0 GB swap, and 247 tasks; multiple scopes served the same roots. Launcher cleanup normally stops a scope, but `SIGKILL` prevents its trap from running, and supervisor reconciliation handles pending diagnoses and stale freezes rather than populated scopes whose encoded owner PID no longer exists.
- Aggregate enforcement did not select one of those command scopes because the supervisor only compares `opencode-tools.slice/memory.current` with its 6.5 GiB `memory.high`. It does not inspect `memory.swap.current`; the slice could therefore sit at its 1 GiB `MemorySwapMax` while resident memory remained below the trigger. The swap controller had rejected more than 700,000 additional swap attempts by investigation time, but reaching `MemorySwapMax` does not invoke the supervisor's deterministic victim selection.
- `ManagedOOMPreference=avoid` did not protect `opencode.service` from the system-wide swap action. On systemd 255, oomd respects `avoid` or `omit` for swap ranking only when the candidate cgroup is root-owned. The monitored `user-1000.slice` is root-owned, but this user service's cgroup is owned by UID 1000, so the preference was ignored and oomd ranked the service normally by its 1.3 GB swap use.
- As an immediate mitigation on 2026-08-18, six confirmed orphaned LSP scopes belonging to dead OpenCode PID 817179 were stopped without touching current OpenCode, BG, ADB, or Browser work. `opencode-tools.slice` fell from about 1.68 GB resident memory, 1.0 GB swap, and 265 tasks to 739 MB resident memory, 12 MB swap, and 31 tasks. Host swap subsequently showed 1.7 of 4.0 GiB in use, and `tool-use lsp status` reported no daemon so later LSP use can auto-start a clean singleton.
- At 2026-08-13 18:16:44 EEST, the separated `opencode.service` was itself selected by `systemd-oomd`; systemd reported nine killed processes, an 8.2 GB memory peak, and a 1.6 GB swap peak, then restarted the service at 18:16:59. The linked parent session `ses_005d7a286ffeinGivaSqt4ujE4` had been waiting since 15:54:20 on its OOM-coordinator subagent, whose mocked freezer probe had remained running since 16:41:13. The kill removed the in-flight requests, but their unfinished assistant and tool records remained in the database. A continuation prompt at 18:19:52 launched replacement subagent work, confirming that the session required explicit recovery rather than automatic process resumption. The live status endpoint subsequently reported the parent and replacements as busy; persisted `running` tool parts alone are therefore not authoritative evidence that the pre-kill process survived.
- The OOM-coordinator subagent was blocked by its temporary `/tmp/opencode/fc018-probe/run.sh` harness, not by an observed production-supervisor deadlock. The harness emulated `cgroup.freeze` with an endless background watcher. Its stale-freeze case verified the resulting `frozen 0` state but also required a redundant mock `systemctl thaw` action even though the supervisor intentionally thawed through a direct cgroup write. That assertion failed under `set -e`; because watcher cleanup existed only in `setup_probe` and at the successful end of the script, the watcher retained the captured output stream indefinitely even after the bounded parent command exited. An explicit timed rerun reproduced the same surviving watcher. The harness needs unconditional `EXIT` cleanup and should assert authoritative cgroup state rather than an implementation-specific mock action.
- The surviving watcher was also the direct driver of the 2026-08-13 OpenCode OOM. Running the harness through `bash -x` traced the 5 ms watcher loop; OpenCode capped the current tool output near 30 KB but appended a complete `message.part.updated.1` event for every update. The affected part produced 173,726 events totaling 5,550,014,078 bytes, compared with a final 31,752-byte part row. The session accounted for 5,588,111,128 bytes across 178,263 part-update events, and the event table occupied 7.5 GB of the 8.6 GB database. The command scope itself remained small; amplification occurred in the parent OpenCode process and bypassed per-command memory limits until `systemd-oomd` killed `opencode.service`. Bounded command execution must therefore terminate descendant output producers and event persistence must not retain unbounded cumulative snapshots.
- The runtime launcher now limits each command tree to 16 MiB of combined stdout/stderr by default. It forwards the first output promptly, then batches later output in full 64 KiB chunks or at command completion. At the boundary it records an `output-limit` diagnosis, kills the scope, and drains the transport before returning status 125. This bounds both the bytes and update count entering OpenCode's repeated output-event path; OpenCode still needs its own persistence compaction upstream.
- Recovery on 2026-08-13 explicitly aborted the affected OOM-coordinator child and stopped its orphaned transient scope. The external probe gained unconditional `EXIT`, signal, and background-job cleanup; its redundant mock-action assertion was removed in favor of the authoritative `frozen 0` state. A bounded untraced run then passed every mocked case, including intentional supervisor `SIGKILL` before and after victim selection, and left no probe process behind. The parent session resumed without another prompt, completed its focused suites, workspace type-check and lint, production web build, syntax checks, documentation lint, dead-code scan, and entered its final cumulative review with OpenCode memory stable and no additional service restart.
- At 2026-08-11 21:10:36 EEST, the user systemd journal recorded `systemd-oomd killed 30 process(es) in this unit` for `openchamber.service`. The unit reported an 8.3 GB memory peak and 338.3 MB swap peak.
- The pre-kill OpenCode log showed several concurrent sessions and, immediately before the kill, parallel UI/web type checks, lints, focused tests, and documentation checks. Other long-lived OpenChamber and development server instances increased the machine's baseline memory use, but the journal identifies memory pressure and the selected official service cgroup as the terminating event.
- Systemd restarted `openchamber.service` at 21:10:42. Its managed OpenCode server became ready on port `46361` by 21:10:45, and `http://127.0.0.1:8792/health` subsequently reported both OpenChamber and OpenCode healthy.
- The shared `~/.local/share/opencode/opencode.db` remained readable. A read-only query found six assistant messages created before the kill without `time.completed`; three retained tool parts recorded as `running`. The live `/session/status` authority after restart correctly omitted those dead requests, so they were idle rather than active and could not make further progress.
- The affected top-level sessions included `Restart openchamber CUSTOM only`, `Debugging duplicated conversation responses`, `Simplifier agent session impact analysis`, `YouTube extension mobile UI fixes`, and `OpenChamber custom settings and restart button`; the latter also owned an unfinished explore subagent.
- The fork's unexpected-interruption behavior compared an unfinished message with a successful snapshot timestamp, but `useSessionActivity` returned idle before that check whenever the directory store contained an explicit idle status. Authoritative reconciliation writes exactly that idle state when an earlier busy session is absent from a snapshot, suppressing the warning in this restart path.
- The UI fix retains the active IDs from each successful authoritative status snapshot. An unfinished turn is interrupted only when it predates a snapshot that omitted it, independent of cached idle representation. A snapshot that included the session remains distinguishable from a later ordinary idle event, avoiding false interruption during normal message-completion races.
- Operational recovery is to inspect each affected worktree and send a new continuation prompt. In-flight shell commands may have completed partially before the cgroup kill, so recovery must not assume success or blindly rerun destructive operations. Resume memory-heavy sessions sequentially on this 16 GB host.
- Follow-up prevention research against OpenCode 1.18.10 confirmed that `tool.execute.before` may mutate bash arguments, but OpenCode runs its command parser and permission checks after that hook. A plugin that rewrites `command` to a `systemd-run` invocation would therefore make permission rules and prompts describe the wrapper rather than the model-requested command.
- OpenCode's `shell` configuration is a safer no-upstream-change boundary. A plugin config hook can select an executable shell launcher while leaving the tool argument unchanged; the launcher can `exec systemd-run --user --scope` around the real non-interactive shell. OpenCode then parses and approves the original command, while its existing working-directory, output capture, timeout, and process-group cancellation remain outside the launcher boundary.
- Live shell-tool probes successfully created transient scopes with `MemoryAccounting=yes`, `MemoryMax=64M`, `MemorySwapMax=0`, and `OOMPolicy=kill`, preserving the working directory and combined stdout/stderr. OpenCode's one-second timeout killed a scoped sleep without leaving a unit or process behind. A 256 MB allocation was OOM-killed inside its 64 MB scope immediately and both OpenChamber services survived, but `systemd-run --scope --collect` did not return to OpenCode until the outer 120-second tool timeout. The naive wrapper is therefore unsuitable for rollout: a launcher must avoid or independently resolve that systemd 255 failure-path hang, and actual limits still need validation.
- A follow-up probe explained the apparent failure-path inconsistency: with `MemoryHigh=32M` and `MemoryMax=64M`, the allocator remained throttled near 34 MB for more than ten seconds with `MemoryAvailable=0`, never reached the hard limit, and emitted no OOM result. Setting the soft and hard limits equal produced an immediate kill. The initial runtime therefore omitted lower `MemoryHigh` thresholds and supervised blocking `systemd-run --scope` without `--collect`; the hard `MemoryMax` boundary returned promptly, while the supervisor propagated exit status, handled cancellation, and cleared the failed transient unit.
- The first FC-018 completion put aggregate monitoring in a child of each command wrapper. A monitor `SIGKILL` between freezing `opencode-tools.slice` and thawing it could therefore leave command scopes frozen forever: the parent wrapper was waiting on `systemd-run`, and no independent process necessarily owned recovery. Moving arbitration into restartable `opencode-tool-memory-supervisor.service` outside the tools slice resolves that lifecycle flaw. The supervisor and admission path share a lock, and every admission reconciles stale state before starting a scope. An atomic pending marker distinguishes a stale freeze before victim selection, which is simply thawed, from one after selection, which completes the selected kill and final diagnosis before thawing.
- The corrected FC-018 aggregate path freezes the tools slice, validates populated systemd scopes, compares hierarchical cgroup `memory.current`, marks and kills the largest command tree with deterministic name-based tie-breaking, and thaws survivors. The victim emits the finalized diagnosis before transient-unit cleanup; per-command `Result=oom-kill` has a separate explicit diagnosis. Unreliable selection or thaw failure marks and terminates all active tool commands rather than guessing or leaking frozen processes.
- Per-command `MemoryMax` contains one pathological command. The aggregate tools slice normally resolves pressure at 6.5 GiB, before its 7 GiB `MemoryMax`, but the hard parent boundary remains an emergency backstop. A rapid allocation or failed monitor can reach that boundary first, where kernel/systemd OOM semantics may affect more than the largest command scope. The runtime therefore does not present aggregate handling as strict per-command isolation.
- Separating OpenCode from OpenChamber also requires external plugin provisioning to retain the OpenChamber agent tool and optional system-prompt optimizer. The minimal secure local design is a single designated OpenChamber control owner, a stable plugin loaded by external OpenCode, and an atomically replaced mode-0600 connection file that the tool reads for each call. This lets the owner rotate its loopback endpoint token after an OpenChamber restart without restarting OpenCode. It does not solve remote-host injection or multiple competing OpenChamber owners.
- Multiple OpenChamber instances may safely observe one OpenCode only when OpenChamber-owned side effects have a single owner. Every server process starts its own scheduled-task runtime, whose timers, queue, running set, and concurrency guards are process-local; two instances loading the same project task definitions can dispatch the same scheduled task twice. Settings and project-config writes are atomically replaced but serialized only by process-local promises, so instances sharing the default `~/.config/openchamber` files can also lose concurrent read-modify-write updates. Agent-tool endpoint/token ownership, OpenCode restart/configuration, notifications, tunnels, and permission responses have the same single-owner or explicit-coordination requirement.

## Deferred follow-up

On 2026-08-18, the user deferred the cross-tool resource-management implementation because budget was not currently available. The canonical findings and proposal are preserved in the `agents-md` repository at `docs/tools/shared-agent-tool-resource-pool.md`. The proposal uses one bounded parent pool, one independently attributable leaf per command or long-lived tool consumer, stable resident-plus-swap largest-victim selection, and lease-based idle expiry. It explicitly does not claim to contain OpenCode-native context or compaction memory.

Evidence sources: `journalctl --user -u openchamber.service`, `systemctl --user status/show openchamber.service`, `oomctl`, `http://127.0.0.1:8792/health`, `~/.local/share/opencode/log/opencode.log`, and read-only queries against `~/.local/share/opencode/opencode.db`.
