# Fork Changes

- **Type:** reference
- **Purpose:** Active index of behavior, fixes, and implementation changes that distinguish `mdatsev/openchamber` from upstream OpenChamber.
- **When to read:** Before implementing fork-only behavior, reviewing the fork's divergence, or integrating an upstream release.
- **Related:** intent.md

## Active Changes

| ID | Change | Reason | Owning paths | Upstream status and removal trigger |
|---|---|---|---|---|
| `FC-001` | Source-run Desktop checks upstream releases for notification only and disables installation with instructions to merge the release into the custom branch. | Keep source-based fork installations informed without replacing them with official upstream binaries. | `packages/electron/main.mjs`, `packages/electron/preload.mjs`, `packages/ui/src/lib/desktop.ts`, `packages/ui/src/stores/useUpdateStore.ts`, `packages/ui/src/components/ui/UpdateDialog.tsx`, `packages/ui/src/lib/i18n/messages/`, `packages/electron/README.md` | Remove when upstream authoritatively distinguishes source runs from packaged installs and provides equivalent notification-only update behavior. |
| `FC-002` | Desktop and dynamic renderer window titles identify the fork as `OpenChamber CUSTOM`. | Distinguish source-run fork windows from the official packaged application. | `packages/electron/main.mjs`, `packages/ui/src/hooks/useWindowTitle.ts` | Remove if upstream provides equivalent configurable application-title branding; stop and ask if upstream changes window-title ownership or formatting. |
| `FC-003` | Source runs install as a distinct `OpenChamber CUSTOM` application on Linux and macOS, with silent non-HMR launchers that content-hash-cache bundled UI assets and write the current run log to a file. | Allow the official packaged application and source fork to be pinned separately while preventing source edits from reloading the launched app, avoiding unchanged rebuilds, and preserving startup failures and runtime logs without opening a terminal. | `package.json`, `scripts/install-custom.sh`, `packages/electron/main.mjs`, `packages/electron/scripts/build-web-assets.mjs`, `packages/electron/scripts/electron-dev.mjs`, `packages/electron/scripts/launch-custom-linux.sh`, `packages/electron/README.md`, `docs/fork-runtime-state.md` | Remove if upstream supports configurable development desktop identity and a logged cached non-HMR source launcher with equivalent behavior. |
| `FC-004` | The Changes Walkthrough provides source-backed diff search with `Ctrl+F`/`Cmd+F`, all-occurrence substring highlighting, and stronger active-match navigation across virtualized diff blocks. | Make walkthrough code searchable despite the diff renderer's shadow DOM and virtualization boundaries. | `packages/ui/src/components/views/walkthrough/`, `packages/ui/src/components/views/PierreDiffViewer.tsx`, `packages/ui/src/lib/i18n/messages/` | Remove when upstream provides equivalent walkthrough-scoped diff search and navigation. |
| `FC-005` | Pending agent questions, including questions from descendant subagents, display a question icon instead of the unread dot or busy spinner in session rows, switchers, folders, groups, and collapsed project indicators. | Distinguish an agent waiting for user input from one that is actively working or has an ordinary unread update, even when the visible parent is waiting on a hidden child session. | `packages/ui/src/sync/sync-context.tsx`, `packages/ui/src/hooks/useSessionActivity.ts`, `packages/ui/src/hooks/useAssistantStatus.ts`, `packages/ui/src/components/session/`, `packages/ui/src/components/session/sidebar/` | Remove when upstream gives authoritative pending-question attention distinct presentation and precedence over busy activity on the same surfaces. |
| `FC-006` | An unfinished assistant turn that predates an authoritative status snapshot and is absent from its active-session set displays a static unexpected-interruption warning in the chat footer and sidebar instead of stale live activity. | Distinguish an agent terminated by an OpenCode restart or similar abnormal interruption from one that is still running. | `packages/ui/src/sync/`, `packages/ui/src/hooks/useSessionActivity.ts`, `packages/ui/src/hooks/useAssistantStatus.ts`, `packages/ui/src/components/chat/`, `packages/ui/src/components/session/sidebar/`, `packages/ui/src/lib/i18n/messages/` | Remove when upstream authoritatively classifies and presents interrupted turns without deriving live activity from stale message history. |
| `FC-007` | Background Task calls made by a subagent appear as children in the spawning Task's chat summary. | Keep asynchronously delegated work visible under the Task that owns its child session. | `packages/ui/src/components/chat/message/parts/taskToolModel.ts`, `packages/ui/src/components/chat/message/parts/DOCUMENTATION.md` | Remove when upstream includes background Task calls in parent Task child-session summaries. |
| `FC-008` | `/side` and `/btw` open one disposable contextual chat beside the main conversation on web and Desktop, with authoritative hiding, cleanup recovery, active-work confirmation, and promotion to a normal session. | Allow tangential interactive work without interrupting or polluting the main conversation. | `packages/ui/src/components/chat/`, `packages/ui/src/components/layout/`, `packages/ui/src/lib/sideChats/`, `packages/ui/src/stores/`, `packages/ui/src/sync/`, `packages/web/server/lib/side-chats/` | Replace with upstream implementation and remove this entry when [openchamber/openchamber#2370](https://github.com/openchamber/openchamber/pull/2370) merges or upstream ships equivalent disposable side chats. |
| `FC-009` | Reverting one unstaged file from the Changes panel requires confirmation that names the file and warns that the local changes cannot be recovered. | Prevent an easy adjacent-button misclick from immediately destroying uncommitted work. | `packages/ui/src/components/views/git/ChangesPanel.tsx`, `packages/ui/src/lib/i18n/messages/` | Remove when upstream requires equivalent confirmation before single-file revert. |
| `FC-010` | Authenticated stat, text-read, and raw-read APIs can access any file readable by the server process while writes and static serving remain workspace-bound. | Authenticated users can already run arbitrary commands and move a file into the workspace, so the read boundary adds friction without preventing access. | `packages/web/server/lib/fs/routes.js`, `packages/web/server/lib/fs/DOCUMENTATION.md` | Remove when upstream permits equivalent authenticated outside-workspace file reads. |
| `FC-011` | Successful final responses create durable per-session unread state, Mark as read/unread and pins synchronize across clients connected to one OpenChamber server, and mobile exposes the same actions. Running and persistent error indicators remain separate. | Make sessions behave like an inbox across Desktop, web, hosted mobile, and Capacitor without allowing delayed reads to clear newer completions. | `packages/web/server/lib/session-inbox/`, `packages/web/server/index.js`, `packages/ui/src/stores/useSessionInboxStore.ts`, `packages/ui/src/sync/`, `packages/ui/src/components/session/`, `packages/ui/src/apps/MobileSessionsSheet.tsx`, `packages/ui/src/lib/i18n/messages/`, `docs/fork-runtime-state.md` | Replace with upstream when it provides durable cross-client unread and pin authority with equivalent completion/error semantics and race-safe read acknowledgements. |
| `FC-012` | Hosted and Capacitor mobile root-chat swipe actions can create a worktree and move the same chat subtree and checkout changes there. | Provide mobile parity with the existing web and Desktop same-session worktree relocation. | `packages/ui/src/apps/MobileSessionsSheet.tsx`, `packages/ui/src/lib/worktrees/sessionWorktreeMove.ts`, `packages/mobile/README.md` | Remove when upstream mobile exposes equivalent same-session worktree relocation. |
| `FC-013` | Tool-call patch previews highlight changed words within paired lines up to 1,000 characters and mark longer skipped lines with an accessible warning icon. | Make small edits immediately legible without reintroducing unbounded intraline-diff work on pathological lines or full-file diff surfaces. | `packages/ui/src/components/chat/message/`, `packages/ui/src/lib/i18n/messages/` | Remove when upstream provides equivalent bounded intraline highlighting and skipped-line disclosure for tool-call patches. |

## Maintenance

- Record every fork-only source, configuration, build, or user-visible behavior change when it is introduced. Keep related edits that implement one capability in one entry.
- Keep entries concise and current. Record owning paths and an upstream issue or pull request when one is known.
- This is an active divergence index, not a changelog. Do not record upstream-only changes, routine synchronization merges, temporary work, generated output, or policy-only documentation.
- Remove an entry when its fork implementation has been removed. Git history preserves the previous record.
- Before integrating an upstream release, compare its changes with every active entry.
- When upstream provides the same fix or behavior, remove the fork implementation, use the upstream implementation, validate the affected behavior, and remove the entry.
- When upstream provides similar but different behavior, changes the same owning area, or leaves equivalence uncertain, stop the integration and ask the user how to proceed.
- Do not retain redundant fork code as a compatibility layer after an equivalent upstream implementation is adopted.
